# Iceberg Metadata Catalog — Python Dependencies
# Tested with Python 3.12 on macOS/Linux
# Install: pip install -r requirements.txt

# Core
boto3>=1.35.0
pyarrow>=17.0.0
pyiceberg[s3tables]>=0.7.0

# Vector Search (OpenSearch Serverless)
opensearch-py>=2.7.0
requests-aws4auth>=1.3.0

# Optional: for demo recording
# asciinema>=2.4.0
