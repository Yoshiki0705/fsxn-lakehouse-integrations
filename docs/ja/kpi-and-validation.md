# KPI と PoC 検証ガイド

## 概要

本ドキュメントは、FSx for ONTAP Lakehouse 統合 PoC の測定可能な成功基準を定義します。明確な KPI がなければ、PoC の成功/失敗を評価することは不可能です。各メトリクスには測定方法と目標範囲を含みます。

## コア KPI

### 1. クエリレイテンシ

| メトリクス | 定義 | 測定方法 | 目標（Good ティア） | 目標（Best ティア） |
|--------|-----------|-------------------|-------------------|-------------------|
| コールドクエリレイテンシ | クエリ送信から最初の結果までの時間（キャッシュなし） | Athena/Databricks クエリ実行時間 | 1 GB スキャンで < 30 秒 | 1 GB スキャンで < 10 秒 |
| ウォームクエリレイテンシ | FSx NVMe/メモリキャッシュにデータがある場合の時間 | 繰り返しクエリ実行時間 | 1 GB スキャンで < 10 秒 | 1 GB スキャンで < 5 秒 |
| メタデータクエリレイテンシ | スキーマ/パーティション検出の時間 | Glue Catalog API レスポンス時間 | < 5 秒 | < 2 秒 |

**測定方法**:
```sql
-- Athena: コンソールまたは API でクエリ実行時間を確認
SELECT query_execution_id, 
       total_execution_time_in_millis,
       data_scanned_in_bytes
FROM information_schema.__query_log__
WHERE query LIKE '%your_table%'
ORDER BY submission_date_time DESC;
```

### 2. スループット

| メトリクス | 定義 | 測定方法 | 目標 |
|--------|-----------|-------------------|--------|
| 読み取りスループット | テーブルスキャン中の持続 MB/s | CloudWatch `DataReadBytes` / クエリ時間 | FSx プロビジョンドスループットの ≥ 80% |
| 書き込みスループット | ETL 書き戻し中の MB/s | CloudWatch `DataWriteBytes` / 書き込み時間 | FSx プロビジョンドスループットの ≥ 50% |
| 同時クエリスループット | 同時負荷下の集約スループット | 複数同時クエリ | プロビジョンド上限まで線形スケーリング |

**注意**: S3 Access Points 経由の S3 API スループットは FSx ファイルシステムのプロビジョンドスループット容量に依存します。ネイティブ S3 のスループットとは同等ではありません。（[ソース](https://docs.aws.amazon.com/fsx/latest/ONTAPGuide/performance.html)）

### 3. スキャンあたりのコスト（TB 単位）

| メトリクス | 定義 | 測定方法 | 目標 |
|--------|-----------|-------------------|--------|
| Athena コスト/TB | スキャンした TB あたりの Athena クエリコスト | $5/TB（Athena 料金）+ FSx スループットコスト | 合計 < $10/TB |
| ストレージコスト/TB | TB あたりの月額ストレージコスト | FSx SSD + キャパシティプール料金 | S3 Standard + ETL パイプラインコストと比較 |
| 総所有コスト | ストレージ + コンピュート + データ転送 | 月額請求分析 | 現在の NAS + S3 コピーパイプラインコスト未満 |

**コスト比較フレームワーク**:
```
Current state cost:
  NAS storage: $X/TB/month
  + S3 copy storage: $Y/TB/month  
  + ETL pipeline compute: $Z/month
  + Data transfer: $W/month
  = Total: $(X+Y+Z+W)/month

FSx S3 AP state cost:
  FSx storage: $A/TB/month
  + Analytics compute: $B/month
  + No S3 copy: $0
  + No ETL pipeline: $0
  = Total: $(A+B)/month

Savings = Current - FSx S3 AP
```

### 4. データ鮮度

| メトリクス | 定義 | 測定方法 | 目標 |
|--------|-----------|-------------------|--------|
| 書き込みからクエリまでのレイテンシ | NFS/SMB 書き込みから S3 AP クエリ可視化までの時間 | NFS 経由でファイル書き込み、即座に S3 AP 経由でクエリ | ほぼゼロ（同一ボリューム、同一データ） |
| カタログ更新レイテンシ | ファイル作成から Glue Catalog 認識までの時間 | Glue Crawler 実行時間またはイベント駆動更新 | < 5 分（Crawler）または < 1 分（イベント） |

**主要な利点**: S3 Access Points は FSx ボリュームから直接読み取るため、NFS/SMB 経由で書き込まれたデータは S3 API 経由で即座に可視化されます。同期遅延なし。

### 5. 回避されたデータコピー数

| メトリクス | 定義 | 測定方法 | 目標 |
|--------|-----------|-------------------|--------|
| コピー数削減 | 排除された冗長データコピーの数 | 現在のデータコピーの棚卸し vs. 新アーキテクチャ | ≥ 50% 削減 |
| ストレージ削減 | 排除された重複ストレージの TB | 排除されたコピーサイズの合計 | $/月で定量化 |
| パイプライン排除 | 不要になった同期/コピーパイプラインの数 | 不要になった ETL ジョブの数 | ≥ 1 パイプライン排除 |

### 6. 目標復旧時間（RTO）/ 目標復旧時点（RPO）

| メトリクス | 定義 | 測定方法 | 目標 |
|--------|-----------|-------------------|--------|
| Snapshot RTO | スナップショットからのボリューム復元時間 | タイム計測付きスナップショット復元テスト | < 5 分 |
| Snapshot RPO | 最大データ損失ウィンドウ | スナップショットスケジュール頻度 | ≤ 1 時間（時間単位スナップショット） |
| SnapMirror RTO | DR リージョンへのフェイルオーバー時間 | タイム計測付きフェイルオーバーテスト | < 15 分 |
| SnapMirror RPO | レプリケーションラグ | SnapMirror ラグメトリクス | < 15 分（非同期）または 0（同期） |

### 7. アクセスポリシーレビュー時間

| メトリクス | 定義 | 測定方法 | 目標 |
|--------|-----------|-------------------|--------|
| ポリシー監査時間 | 全アクセスポリシーのレビュー・検証時間 | IAM + AP + ファイルシステム権限の手動レビュー | アクセスポイントあたり < 1 時間 |
| 権限変更時間 | アクセス付与/取り消しの時間 | リクエストから有効化までの時間 | < 30 分 |
| コンプライアンスレポート生成 | アクセス監査レポートの生成時間 | CloudTrail クエリ + レポート生成 | < 1 時間 |

## PoC 成功基準テンプレート

### フェーズ 1: 接続性検証（Week 1）

| 基準 | 合格条件 | テスト方法 |
|-----------|---------------|-------------|
| S3 AP 作成 | アクセスポイントが AVAILABLE 状態 | `aws fsx describe-s3-access-points` |
| 基本読み取り | AP エイリアス経由で GetObject 成功 | `aws s3 cp s3://AP-ALIAS/test.txt .` |
| 基本書き込み | AP エイリアス経由で PutObject 成功 | `aws s3 cp test.txt s3://AP-ALIAS/` |
| オブジェクト一覧 | ListObjectsV2 が期待されるファイルを返す | `aws s3 ls s3://AP-ALIAS/` |
| IAM 認可 | 権限のないロールが拒否される | AP 権限のないロールでテスト |
| ファイルシステム認可 | 読み取り専用ユーザーが書き込み不可 | 読み取り専用 AP ユーザーで PutObject テスト |

### フェーズ 2: 分析統合（Week 2）

| 基準 | 合格条件 | テスト方法 |
|-----------|---------------|-------------|
| Glue Crawler | Glue Catalog にテーブル登録 | Crawler 実行、カタログでテーブル確認 |
| Athena クエリ | SQL クエリが正しい結果を返す | 登録テーブルに対して SELECT 実行 |
| クエリパフォーマンス | データセットサイズに対するレイテンシ目標を達成 | クエリ実行時間を測定 |
| データ鮮度 | 新しいファイルが SLA 内でクエリに可視 | NFS 経由で書き込み、Athena 経由でクエリ |

### フェーズ 3: 本番準備（Week 3-4）

| 基準 | 合格条件 | テスト方法 |
|-----------|---------------|-------------|
| 同時アクセス | N 同時クエリで劣化なし | 並列クエリによる負荷テスト |
| セキュリティ検証 | 両レイヤーで不正アクセスが拒否 | IAM + ファイルシステムのペネトレーションテスト |
| 監査完全性 | 全アクセスイベントが CloudTrail に記録 | テスト期間の CloudTrail レビュー |
| リカバリテスト | RTO 目標内でのスナップショット復元 | タイム計測付き復元演習 |
| コスト検証 | 総コストが予算内 | 実績 vs. 予測コストの比較 |

## 測定ツール

| ツール | 測定対象 | セットアップ |
|------|-----------------|-------|
| CloudWatch（FSx メトリクス） | スループット、IOPS、レイテンシ、ストレージ使用率 | FSx で自動 |
| CloudTrail | API コール、データアクセスイベント | AP に対する S3 データイベント有効化 |
| Athena クエリ履歴 | クエリレイテンシ、スキャンデータ、コスト | Athena 組み込み |
| Databricks クエリプロファイル | クエリ実行計画、所要時間 | Databricks 組み込み |
| AWS Cost Explorer | 月額コスト内訳 | 自動 |

---

## ビジネス KPI マッピング

技術 KPI は、経営層やビジネスステークホルダーが評価できるビジネス/運用メトリクスに変換する必要があります。

| 技術 KPI | ビジネス KPI | 回答されるビジネス上の問い |
|--------------|-------------|---------------------------|
| クエリレイテンシ（秒） | 分析リクエストから意思決定までの時間 | 「データに基づいてどれだけ速く行動できるか？」 |
| データ鮮度（書き込みからクエリまで） | 運用データ反映遅延 | 「最新のデータで意思決定しているか？」 |
| 回避されたデータコピー数 | ストレージコスト削減（$/月）+ 運用人件費削減 | 「コピーパイプラインを維持しないことでどれだけ節約できるか？」 |
| スループット（MB/s） | アナリスト生産性（クエリ/日） | 「チームは待たずに必要な分析をすべて実行できるか？」 |
| スキャンあたりのコスト（TB） | ビジネスインサイトあたりの分析コスト | 「各レポート/ダッシュボードのコストはいくらか？」 |
| RTO / RPO | 事業継続 SLA 準拠 | 「規制上の復旧コミットメントを満たせるか？」 |
| アクセスポリシーレビュー時間 | 監査対応時間（時間） | 「コンプライアンス監査にどれだけ迅速に対応できるか？」 |
| RAG 回答精度 | ナレッジワーカーの時間節約 | 「従業員はどれだけ速く情報を見つけられるか？」 |

### エグゼクティブダッシュボードメトリクス

CxO レポーティングでは、4〜5 個のヘッドラインメトリクスに集約します：

1. **コスト回避**: データコピーパイプライン排除により $X/月を節約
2. **Time-to-insight**: Y 日から Z 時間に短縮
3. **コンプライアンス態勢**: データアクセスの 100% が CloudTrail で監査可能
4. **AI 準備度**: N 件のドキュメントが移行なしで AI/RAG にアクセス可能
5. **運用リスク**: DR テスト済み、RTO < 15 分、RPO < 1 時間

---

## 実測ベンチマーク結果（2026-05-23）

> **環境**: FSx for ONTAP Single-AZ、128 MB/s プロビジョンドスループット、ap-northeast-1。これらは特定のテスト環境でのサンプル測定値であり、本番環境の見積もりではありません。本番ワークロードにはスループットプロビジョニングをスケールしてください。

### 全プラットフォーム横断ベンチマーク比較

> **注**: これらの測定値はパートナーが現実的な PoC 期待値を設定するためのものです。特定のテスト環境からのサイジングリファレンスであり、サービス制限や本番保証ではありません。

| プラットフォーム | 操作 | データサイズ | レイテンシ | スループット | コスト/クエリ |
|---------------|------|-----------|---------|-----------|-----------|
| **Athena** | フルスキャン（SELECT *） | 103 MB / 500万行 | 2.2秒 | 54.8 MB/s | ~$0.0005 |
| **Athena** | GROUP BY（5カラム） | 37.7 MB スキャン | 3.3秒 | 12.2 MB/s | ~$0.0002 |
| **Athena** | CTAS 書き戻し | 10K行 → 3行 | 3.7秒 | — | ~$0.0001 |
| **DuckDB（ローカル）** | COUNT(*) | 500万行 | 779ms | — | $0 |
| **DuckDB（ローカル）** | GROUP BY | 10K行 | 1.2秒 | — | $0 |
| **DuckDB（ローカル）** | COPY TO Parquet | 集約結果 | 304ms | — | $0 |
| **DuckDB（Lambda、コールド）** | シンプルクエリ | — | 1,854ms | — | ~$0.00003 |
| **DuckDB（Lambda、ウォーム）** | COUNT(*) 10K行 | 10K行 | 452ms | — | ~$0.00001 |
| **DuckDB（Lambda、ウォーム）** | GROUP BY 10K行 | 10K行 | 1,411ms | — | ~$0.00002 |
| **Redshift Spectrum** | COUNT(*) | 10K行 | 3.2秒 | — | ~$0.005 |
| **Redshift Spectrum** | COUNT(*) | 500万行 | 4.3秒 | — | ~$0.005 |
| **Redshift Spectrum** | GROUP BY + AVG | 10K行 | 2.6秒 | — | ~$0.005 |
| **EMR Serverless Spark** | Parquet 読み取り | 10K行 | 6.8秒 | — | ~$0.001 |
| **EMR Serverless Spark** | GROUP BY | 10K行 | 2.5秒 | — | ~$0.001 |
| **EMR Serverless Spark** | 書き戻し | 3行 | 3.6秒 | — | ~$0.001 |
| **Glue ETL** | 読み取り + 変換 + 書き込み | 10K行 | 64秒 | — | ~$0.02 |
| **delta-rs** | DeltaTable.open | 10K行 | 0.91秒 | — | $0 |
| **delta-rs** | to_pyarrow_table | 10K行 | 1.38秒 | — | $0 |

**PoC 計画のための主要ポイント**:
- **最低コスト**: DuckDB Lambda（アイドル $0、~$0.00001/クエリ）— アドホック軽量分析に最適
- **最速大規模スキャン**: Athena（54.8 MB/s）— 大規模データセットのサーバーレス SQL に最適
- **最も柔軟**: EMR Spark（読み取り + 書き込み + Iceberg 読み取り）— ETL パイプラインに最適
- **DWH 統合**: Redshift Spectrum — 既存 Redshift テーブルとの JOIN に最適

### Athena + Parquet 読み取り（103 MB データセット、500 万行、10 カラム）

| クエリタイプ | スキャンデータ | エンジン時間 | 合計時間 | スループット |
|-----------|:---:|:---:|:---:|:---:|
| COUNT(*) (メタデータのみ) | 0 B | 1,123 ms | 1,316 ms | N/A |
| GROUP BY (5 カラム) | 37.7 MB | 3,103 ms | 3,273 ms | 12.2 MB/s |
| SELECT * WHERE + ORDER (全カラム) | 109.5 MB | 2,000 ms | 2,196 ms | **54.8 MB/s** |
| 繰り返しクエリ P50 | 10.1 MB | 1,888 ms | 2,072 ms | 5.3 MB/s |

**KPI 評価**: 103 MB フルスキャン 2.2 秒 → 1 GB 換算で約 20 秒 → **Good ティア目標（< 30 秒）PASS**

### Glue ETL（10K 行、読み取り + 変換 + 書き戻し）

| メトリクス | 値 |
|--------|-------|
| 合計実行時間 | 64 秒 |
| 入力行数 | 10,000 |
| 出力行数 | 約 15（集約後） |
| ワーカータイプ | G.1X × 2 |

### アップロードパフォーマンス（ローカル → FSx S3 AP、インターネット経由）

| ファイルサイズ | 所要時間 | スループット |
|-----------|----------|-----------|
| 250 KB | 1.4 秒 | 179 KB/s |
| 103 MB | 6.3 秒 | 16.5 MB/s |

### Delta Lake OSS 読み取り（delta-rs、10K 行）

| 操作 | レイテンシ |
|-----------|---------|
| DeltaTable.open（メタデータ） | 0.91 秒 |
| to_pyarrow_table（フル読み取り） | 1.38 秒 |
| to_pandas + 集約 | 0.15 秒 |

---

## 初期ユースケース選定

最初の Investment Case では、リスクを最小化しつつ測定可能性を最大化する ONE ユースケースを選択します。

### 決定: Enterprise IT RAG

以下の選定マトリクスに基づき、**Enterprise IT RAG** が推奨される最初のユースケースです。

```yaml
# Value Hypothesis (define BEFORE verification)
value_hypothesis:
  use_case: "Enterprise IT RAG"
  business_issue: "Incident responders spend too long searching for relevant runbooks and past incident reports"
  target_users: "L2 support engineers, SRE team, operations staff"
  data_source: "Runbooks, incident reports, architecture docs, FAQs on NFS/SMB file shares"
  
  expected_value:
    search_time_reduction: "30-50%"
    first_response_time_reduction: "20-30%"
    mttr_improvement: "10-15%"
  
  measurement:
    baseline_period: "30 days before PoC"
    measurement_period: "90 days"
    primary_kpi: "Mean time from incident start to relevant document found"
    secondary_kpis:
      - "MTTR (mean time to resolution)"
      - "User satisfaction (1-5 survey)"
      - "RAG answer accuracy (human review pass rate)"
  
  guardrails:
    data_risk: "Low (no PHI/PII in IT runbooks)"
    read_only: true
    human_review: "Required for first 30 days; optional after accuracy > 90%"
  
  decision_criteria:
    scale: "Search time reduced ≥ 30%; accuracy ≥ 85%; cost within budget"
    adjust: "Technically works but adoption < 50% or accuracy < 85%"
    stop: "No measurable improvement after 60 days"
  
  investment_case_timeline:
    benchmark_complete: "Week 2"
    security_test_complete: "Week 2"
    initial_cost_estimate: "Week 3"
    draft_investment_case: "Day 30 (based on initial usage data)"
    final_investment_case: "Day 90 (based on full measurement period)"
```

### 測定シーケンス

| ステップ | アクティビティ | タイムライン | アウトプット |
|------|----------|----------|--------|
| 1 | ベースライン KPI 測定（現在の検索時間、MTTR） | Week -2 〜 0 | ベースラインメトリクス |
| 2 | データ準備度評価 | Week 1 | 準備度スコア |
| 3 | FSx S3 AP + Athena/Bedrock デプロイ | Week 1-2 | 稼働環境 |
| 4 | 機能 + セキュリティ + ベンチマークテスト | Week 2 | エビデンス記録 |
| 5 | ユーザーパイロット（5-10 名） | Week 3-4 | 初期利用データ |
| 6 | 30 日レビュー + Investment Case ドラフト | Day 30 | Go/Adjust 判断 |
| 7 | 全ユーザー展開（Go の場合） | Day 30-60 | 広範な採用データ |
| 8 | 60 日最適化 | Day 60 | チューニング済みシステム |
| 9 | 90 日 Scale/Adjust/Stop 判断 | Day 90 | 最終 Investment Case |

### 推奨される最初のユースケース

| 基準 | Enterprise IT RAG | 製造業メンテナンス | ヘルスケアリサーチ |
|-----------|:-:|:-:|:-:|
| PHI/PII リスク | 低 | 低 | 中（匿名化が必要） |
| ビジネス KPI の明確さ | 高（MTTR） | 高（ダウンタイム） | 中（研究アウトプット） |
| 30/60/90 日測定可能性 | 高 | 高 | 中 |
| データ準備度（一般的） | 高（NAS 上のドキュメント） | 中（混合フォーマット） | 低（パイプラインが必要） |
| ステークホルダーの複雑さ | 低 | 低 | 高（IRB、倫理） |
| **推奨優先度** | **#1** | **#2** | **#3** |

**推奨事項**: Enterprise IT RAG または製造業メンテナンスから開始してください。ヘルスケアは、低リスクのユースケースでガバナンスフレームワークが検証された後に続けるべきです。

### エグゼクティブ Investment Case インプット要件

Investment Case を作成する前に、以下の測定値を収集します：

| インプット | ソース | 必要な用途 |
|-------|--------|-------------|
| クエリレイテンシ（P50, P95） | ベンチマーク結果 | Time-to-insight の主張 |
| スループット（MB/s） | ベンチマーク結果 | キャパシティプランニング |
| データセットサイズとファイル数 | データ準備度評価 | ストレージコスト予測 |
| ストレージ + クエリ + インジェストコスト | AWS Cost Explorer + Athena 課金 | ROI 計算 |
| 運用工数（時間/週） | 運用チーム見積もり | TCO 比較 |
| データ鮮度（書き込みからクエリまで） | 機能テスト | 鮮度 SLA の主張 |
| エラー/障害率 | テスト結果 | 信頼性の主張 |
| セキュリティテスト結果 | ネガティブテストマトリクス | リスク態勢ステートメント |

### 90 日判断基準

Day 90 で、3 つの判断のいずれかを行います：

| 判断 | 基準 | 次のアクション |
|----------|----------|-------------|
| **Scale** | KPI 改善が確認された（≥ 目標）；リスクとコストが許容範囲内；ユーザー採用 > 50% | より多くのユーザー/データ/ユースケースに拡大；投資のためエグゼクティブに提示 |
| **Adjust** | 技術的には実現可能だが、データ品質、ユーザー採用、またはビジネスフィットに改善が必要 | 具体的なギャップを特定；30 日改善計画を定義；Day 120 で再評価 |
| **Stop** | KPI 改善が達成不可能；運用負荷が高すぎる；リスクが許容不可；コストが価値を超過 | 学びを文書化；環境をアーカイブ；投資を別方向に振り向け |

---

## PoC ガードレール

安全な実験を可能にするため、PoC 中に許可される範囲の明確な境界を定義します。

### PoC で許可されること

| アクティビティ | 条件 |
|----------|-----------|
| 読み取り専用分析（Athena, Glue Crawler） | 任意のデータ分類 |
| Glue ETL 読み取り + 書き戻し（Parquet） | Internal/Public データのみ |
| Bedrock RAG インジェスト | 匿名化済みドキュメントのみ |
| パフォーマンスベンチマーク | 合成データまたは公開データセット |
| セキュリティテスト（IAM 拒否検証） | 制御されたテストプリンシパル |
| スナップショット復元テスト | 非本番ボリューム |

### PoC で許可されないこと

| アクティビティ | 理由 |
|----------|--------|
| Delta Lake write / MERGE / compaction | 非サポート — 不整合な状態を生成する |
| 非本番環境での実際の PHI/PII | コンプライアンス違反リスク |
| Confidential/Regulated データに対するインターネットオリジン AP | 規制データに対するセキュリティ態勢が不十分 |
| テスト用の本番 AP ポリシー変更 | 本番アクセス中断のリスク |
| 定義されたコスト上限の超過 | 予算管理 |
| PoC アクセス認証情報の共有 | 監査証跡の完全性 |

### PoC コスト上限

開始前に最大許容 PoC コストを定義します：

| コンポーネント | 見積コスト（2 週間 PoC） |
|-----------|---------------------------|
| FSx for ONTAP (512 MB/s, 1 TB SSD) | 〜$800 |
| Athena クエリ（10 TB スキャン） | 〜$50 |
| Glue Crawler + ETL | 〜$20 |
| CloudTrail（S3 データイベント） | 〜$10 |
| **合計上限** | **< $1,000** |

### PoC 終了基準

| 結果 | アクション |
|---------|--------|
| フェーズ 1-2 の全基準合格 | フェーズ 3（本番準備）に進む |
| パフォーマンス目標未達 | FSx スループットサイジングを評価；より高いプロビジョニングで再テスト |
| セキュリティ検証失敗 | 進行前に調査・修正 |
| コストが上限を超過 | 停止してアーキテクチャを再評価 |
| 根本的な制限を発見 | 文書化して代替アプローチを評価 |

---

## RAG ユースケースカタログ

FSx S3 AP + Amazon Bedrock Knowledge Bases により、移行なしで既存のエンタープライズファイルデータに対する RAG が可能になります。

| 業界 | ユースケース | ソースドキュメント | ビジネス価値 |
|----------|----------|-----------------|----------------|
| **製造業** | メンテナンスマニュアル / 作業標準検索 | PDF マニュアル、作業指示書、検査記録 | 設備ダウンタイム削減；技術者オンボーディング高速化 |
| **ヘルスケア** | 研究文書 / 匿名化症例検索 | 研究論文、匿名化臨床ノート、プロトコル | 研究加速；エビデンスに基づく意思決定の改善 |
| **金融サービス** | 規制 / 監査証跡 / 契約検索 | コンプライアンス文書、監査報告書、契約書 | 規制対応の高速化；コンプライアンスリスク削減 |
| **メディア** | 過去アセット / メタデータ / スクリプト検索 | 制作ノート、スクリプト、アセットメタデータファイル | コンテンツ発見の高速化；既存アセットの再作成削減 |
| **Enterprise IT** | インシデントレポート / Runbook 検索 | インシデントレポート、Runbook、アーキテクチャドキュメント | インシデント解決の高速化；ナレッジ保全 |

### RAG 実装パターン

```
Existing NFS/SMB workflow          Analytics / AI workflow
        │                                    │
        ▼                                    ▼
┌──────────────────┐              ┌─────────────────────┐
│ Users write docs │              │ Bedrock Knowledge   │
│ via NFS/SMB      │              │ Base ingests via     │
│ (unchanged)      │              │ S3 Access Point      │
└────────┬─────────┘              └──────────┬──────────┘
         │                                    │
         ▼                                    ▼
┌──────────────────────────────────────────────────────┐
│              FSx for ONTAP Volume                     │
│         (single source of truth)                      │
└──────────────────────────────────────────────────────┘
```

**主要な利点**: ユーザーが NFS/SMB 経由で書き込んだドキュメントは、S3 AP 経由で即座に RAG インジェストに利用可能です。コピーなし、同期なし、遅延なし。

---

## 将来: Agentic AI 統合

AI が RAG から自律エージェントへ進化するにつれ、FSx S3 AP はエンタープライズファイルデータを AI エージェント向けのガバナンス付きデータアクセスレイヤーとして位置づけます。

### 潜在的な統合ポイント

| AI 機能 | FSx S3 AP の役割 | ステータス |
|--------------|---------------|--------|
| **Bedrock Knowledge Bases (RAG)** | 検索用ドキュメントデータソース | ✅ 利用可能（[チュートリアル](https://docs.aws.amazon.com/fsx/latest/ONTAPGuide/tutorial-build-rag-with-bedrock.html)） |
| **Bedrock Agents (tool use)** | エージェントが S3 API ツール経由でドキュメントを取得 | アーキテクチャ的に実現可能（agent → S3 GetObject → AP → FSx） |
| **MCP 互換ツール** | 任意の MCP クライアント向け S3 ベースドキュメント取得ツール | アーキテクチャ的に実現可能（MCP tool wraps S3 GetObject） |
| **Athena クエリツール** | エージェントが Athena + Glue Catalog 経由でファイルデータに SQL 実行 | アーキテクチャ的に実現可能（agent → Athena → Glue → AP → FSx） |
| **マルチモーダル AI** | メディアファイルの画像/動画/音声分析 | アーキテクチャ的に実現可能（agent → GetObject → media file → model） |

### Agentic AI のガバナンス

AI エージェントがエンタープライズデータに自律的にアクセスする場合、ガバナンスが重要になります：

- **読み取り専用アクセスポイント**: エージェントはソースデータへの書き込みアクセスを決して持つべきではない
- **スコープされた IAM ロール**: エージェントごとの IAM ロールで最小限必要な権限のみ
- **監査証跡**: 全エージェントデータアクセスを CloudTrail S3 データイベントで記録
- **Human-in-the-loop**: ビジネス判断に影響するエージェントアクションには人間の承認が必要
- **レート制限**: 暴走エージェントが FSx スループットを圧迫することを防止
- **データ分類認識**: エージェントはデータ分類の境界を尊重する必要がある

### 戦略的ポジショニング

```
Today (2025):          Near-term (2026):           Future:
RAG on documents  →    Agents query structured  →  Autonomous agents
(Bedrock KB)           + unstructured data          with governed access
                       (Athena + Bedrock)           to enterprise files
```

FSx S3 AP は、データセキュリティと監査可能性が譲れない AI エージェントアーキテクチャに適した**ガバナンス付き、監査可能、読み取り専用のデータアクセスレイヤー**を提供します。

### Agentic AI ロードマップ（フェーズ別）

| フェーズ | 機能 | ビジネスユースケース | ガードレール | 人間の承認 | 終了基準 |
|-------|-----------|-------------------|-----------|----------------|---------------|
| **1. 読み取り専用 RAG** | FSx ドキュメント上の Bedrock Knowledge Base | ドキュメント検索、マニュアル/ポリシーの Q&A | 読み取り専用 AP、回答の人間レビュー | 全レスポンスに必須 | 精度 > 80%、ユーザー採用 > 50% |
| **2. RAG + クエリアシスタント** | RAG + Athena SQL 生成 | 「先月の生産データを見せて」→ SQL → 結果 | 読み取り専用 AP + Athena、クエリコスト制限 | データ変更クエリに必須 | クエリ精度 > 90%、コスト < 予算 |
| **3. エージェント支援分析** | マルチステップ: ドキュメント取得 → データクエリ → 要約 | 自動レポート生成、トレンド分析 | 読み取り専用 AP、実行時間制限、出力レビュー | レポート配布前に必須 | ドメインエキスパートによるレポート品質検証 |
| **4. マルチツールガバナンスエージェント** | エージェントが複数ツールを使用（S3, Athena, Bedrock, Lambda） | 複雑なリサーチワークフロー、クロスソース分析 | ツールごとの IAM スコーピング、レート制限、監査 | ビジネスインパクトのあるアクションに必須 | ワークフロー完了率 > 95% |
| **5. 人間承認付き自律** | エージェントがアクションを提案、人間が実行を承認 | 自動データパイプライン管理、異常対応 | 完全な監査証跡、ロールバック機能、SLA モニタリング | 不可逆アクションのみ承認必要 | 平均解決時間が > 50% 改善 |

---

## 本番採用計画

### PoC → 本番移行

| ステップ | アクティビティ | オーナー | 期間 | ゲート |
|------|----------|-------|----------|------|
| 1 | **PoC 成功判断** | ビジネススポンサー + テクニカルリード | 1 日 | 全 PoC 終了基準を達成 |
| 2 | **本番準備レビュー** | アーキテクチャレビューボード | 1 週間 | セキュリティ、パフォーマンス、コスト承認 |
| 3 | **セキュリティレビュー** | CISO / セキュリティチーム | 1-2 週間 | Security Verified 基準を達成 |
| 4 | **データオーナー承認** | データガバナンス委員会 | 1 週間 | データ分類確認、アクセス承認 |
| 5 | **コスト承認** | 財務 / 予算オーナー | 1 週間 | 月額コストが予算内 |
| 6 | **運用引き継ぎ** | プラットフォームチーム | 1-2 週間 | Runbook 文書化、モニタリング設定完了 |
| 7 | **ユーザーオンボーディング** | データプラットフォームチーム | 1 週間 | ユーザートレーニング完了、アクセスプロビジョニング |
| 8 | **KPI ベースライン** | アナリティクスチーム | 1 週間 | ベースラインメトリクス記録 |
| 9 | **30 日バリューチェック** | ビジネススポンサー | Day 30 | KPI がポジティブトレンド |
| 10 | **60 日最適化** | プラットフォームチーム | Day 60 | パフォーマンスチューニング、コスト最適化 |
| 11 | **90 日バリューレポート** | ビジネススポンサー | Day 90 | ビジネス価値定量化、拡大判断 |

### 本番準備チェックリスト

- [ ] 全 PoC 成功基準が文書化され達成済み
- [ ] Security Verified ステータス達成（compatibility-matrix.md 参照）
- [ ] 運用 Runbook テスト済み（compatibility-matrix.md 参照）
- [ ] モニタリングとアラート設定済み（CloudWatch, CloudTrail）
- [ ] バックアップとリカバリテスト済み（RTO 内でのスナップショット復元）
- [ ] コストモデル検証済み（実績 vs. 予測）
- [ ] ユーザーアクセスプロビジョニング済み（IAM ロール、AP ポリシー）
- [ ] ドキュメント完成（アーキテクチャ、運用、ガバナンス）
- [ ] インシデント対応手順レビュー済み
- [ ] DR 手順テスト済み（該当する場合）

---

## 継続的改善ループ

本番デプロイは終わりではなく、価値創造の始まりです。

### 月次改善サイクル

```
┌─────────────────────────────────────────────────────────┐
│                                                          │
│  ┌──────────┐    ┌──────────┐    ┌──────────────┐      │
│  │ 1.MEASURE│───▶│ 2.ANALYZE│───▶│ 3.IDENTIFY   │      │
│  │ Usage &  │    │ Trends & │    │ Bottlenecks  │      │
│  │ Outcomes │    │ Anomalies│    │ & Opportunities│     │
│  └──────────┘    └──────────┘    └──────┬───────┘      │
│                                          │               │
│  ┌──────────┐    ┌──────────┐    ┌──────▼───────┐      │
│  │ 7.REPORT │◀───│ 6.VERIFY │◀───│ 4.IMPROVE    │      │
│  │ Monthly  │    │ Results  │    │ Tune config  │      │
│  │ Value    │    │          │    │ / data / cost│      │
│  └──────────┘    └──────────┘    └──────────────┘      │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### 月次測定項目

| カテゴリ | メトリクス | 目標トレンド |
|----------|--------|-------------|
| **利用状況** | クエリ/日、ユーザー/週、スキャンデータ/月 | 増加 |
| **パフォーマンス** | P50/P95 クエリレイテンシ、スループット使用率 | 安定または改善 |
| **コスト** | $/TB スキャン、$/クエリ、月額合計コスト | 単位あたり減少 |
| **ビジネス価値** | 可能になった意思決定、節約時間、排除されたパイプライン | 増加 |
| **品質** | クエリエラー、失敗ジョブ、データ鮮度 SLA 違反 | 減少 |
| **セキュリティ** | 不正アクセス試行、ポリシードリフトイベント | ゼロまたは減少 |
| **AI（RAG の場合）** | 回答精度、ユーザー満足度、レビュー却下率 | 改善 |

### 月次バリューレポートテンプレート

```markdown
# Monthly Value Report: FSx for ONTAP Lakehouse Integration
## Period: [Month Year]

### Executive Summary
- Total queries: X (+Y% vs last month)
- Active users: N
- Data copies eliminated: Z TB
- Estimated cost savings: $W

### KPI Dashboard
| KPI | Target | Actual | Trend |
|-----|--------|--------|-------|
| Query latency P50 | < 10s | Xs | ↑↓→ |
| Cost per TB | < $10 | $X | ↑↓→ |
| Data freshness | < 5 min | X min | ↑↓→ |

### Issues & Actions
| Issue | Impact | Action | Owner | Due |
|-------|--------|--------|-------|-----|

### Next Month Focus
- [Optimization opportunity]
- [Expansion opportunity]
```

---

## データ準備度評価

FSx S3 AP を分析や AI に展開する前に、データの準備状況を評価します。

### 評価ディメンション

| ディメンション | 質問 | スコア (1-5) | 低い場合のアクション |
|-----------|----------|-------------|---------------|
| **ファイルフォーマット準備度** | ファイルは分析に適したフォーマット（Parquet, ORC, JSON, CSV）か？ | | 最高のパフォーマンスのために Parquet に変換 |
| **メタデータ可用性** | ファイルに一貫した命名、パーティショニング、またはメタデータがあるか？ | | 命名規則を実装；Glue Crawler を追加 |
| **アクセスオーナーシップ** | 分析アクセスを承認できる明確なデータオーナーがいるか？ | | データオーナーを特定し文書化 |
| **データ分類** | データは分類されているか（Public/Internal/Confidential/Regulated）？ | | S3 AP 経由で公開する前に分類 |
| **鮮度** | データはどの程度最新か？分析に対して陳腐化は許容可能か？ | | 鮮度 SLA を文書化 |
| **重複データ** | 統合可能な冗長コピーがあるか？ | | コピーの棚卸し；統合計画 |
| **機密データ比率** | PHI/PII/シークレットを含む割合は？ | | 分析用に > 0% の場合は匿名化パイプライン |
| **検索可能性** | ドキュメントは RAG 用に意味のある検索/チャンキングが可能か？ | | ドキュメント構造を評価；チャンキングをテスト |
| **カタログ準備度** | Glue Crawler がデータを正常にカタログ化できるか？ | | Crawler をテスト；フォーマット問題を修正 |
| **ボリュームサイズ** | データセットサイズは FSx スループットプロビジョニングに適切か？ | | FSx スループットをデータセットに合わせてサイジング |

### 準備度スコアリング

| 合計スコア | 準備度レベル | 推奨事項 |
|-------------|----------------|----------------|
| 40-50 | 準備完了 | 即座に PoC に進む |
| 30-39 | ほぼ準備完了 | 1-2 のギャップに対処してから PoC |
| 20-29 | 部分的に準備完了 | 大幅な準備が必要（2-4 週間） |
| 10-19 | 準備未完了 | まず大規模なデータエンジニアリング作業が必要 |

### 評価アウトプット

```yaml
assessment_date: "YYYY-MM-DD"
assessor: "<name>"
data_source: "<volume/path>"
total_score: X/50
readiness_level: "Ready | Mostly ready | Partially ready | Not ready"
blockers:
  - "<blocker 1>"
  - "<blocker 2>"
recommended_actions:
  - action: "<action>"
    effort: "<days>"
    owner: "<team>"
poc_ready: true/false
estimated_prep_time: "X weeks"
```

---

## ユースケース KPI ツリー

各ユースケースについて、ビジネス課題から測定可能な目標までの完全なチェーンを定義します。

### Enterprise IT: Runbook RAG

| レベル | メトリクス | 測定方法 | 目標 |
|-------|--------|-------------|--------|
| **ビジネス課題** | インシデント解決に時間がかかりすぎる | チケットシステムからの MTTR | ベースライン: X 時間 |
| **プロセス KPI** | Runbook/手順の検索時間 | 検索開始から関連ドキュメント発見までの時間 | 90 日で 50% 削減 |
| **アウトカム KPI** | 平均解決時間（MTTR） | チケットクローズ時間 - チケットオープン時間 | 90 日で 15% 削減 |
| **エクスペリエンス KPI** | オペレーター満足度 | アンケート（1-5 スケール） | ≥ 4.0 |
| **品質 KPI** | RAG 回答精度 | 人間レビュー合格率 | ≥ 85% |
| **リスク KPI** | ハルシネーション / 引用ミス率 | レビュー却下率 | < 10% |

### 製造業: メンテナンスマニュアル検索

| レベル | メトリクス | 測定方法 | 目標 |
|-------|--------|-------------|--------|
| **ビジネス課題** | マニュアル検索の遅さによる設備ダウンタイム | ダウンタイム時間/月 | ベースライン: X 時間 |
| **プロセス KPI** | マニュアル/手順の取得時間 | 関連するメンテナンス指示を見つけるまでの時間 | 90 日で 60% 削減 |
| **アウトカム KPI** | 設備可用性 | 稼働率 % | 5% 改善 |
| **エクスペリエンス KPI** | 技術者満足度 | アンケート | ≥ 4.0 |
| **リスク KPI** | 誤った手順の実行 | 誤った手順を引用したインシデントレポート | ゼロ |

### ヘルスケア: 研究文書検索

| レベル | メトリクス | 測定方法 | 目標 |
|-------|--------|-------------|--------|
| **ビジネス課題** | 研究者が関連研究の発見に時間をかけすぎている | 文献検索の時間/週 | ベースライン: X 時間/週 |
| **プロセス KPI** | ドキュメント発見時間 | クエリから関連ドキュメントセットまでの時間 | 70% 削減 |
| **アウトカム KPI** | 研究アウトプット | 提出論文数/四半期 | 20% 増加 |
| **エクスペリエンス KPI** | 研究者満足度 | アンケート | ≥ 4.0 |
| **リスク KPI** | 研究コンテキストでの PHI 露出 | 監査所見 | ゼロ |

---

## 30/60/90 日バリュー実現計画

### Day 30: 採用とベースライン

| チェック | 質問 | エビデンス | 判断 |
|-------|----------|----------|----------|
| 利用採用 | ターゲットユーザーが積極的にクエリしているか？ | クエリ数、ユニークユーザー | 採用 < 50% の場合: 障壁を調査 |
| KPI ベースライン | 信頼性のあるベースライン測定があるか？ | Before/After 比較データ | ベースラインなしの場合: 測定期間を延長 |
| 重大な問題 | ブロッキングとなる技術的またはセキュリティ上の問題があるか？ | 課題トラッカー、インシデントログ | 重大な問題がある場合: 一時停止して修正 |
| ユーザーフィードバック | ユーザーはエクスペリエンスについて何と言っているか？ | アンケート、インタビュー | フィードバックを最適化に反映 |

**30 日判断**: 計画通り継続 / スコープ調整 / 問題エスカレーション

### Day 60: 最適化とエビデンス

| チェック | 質問 | エビデンス | 判断 |
|-------|----------|----------|----------|
| パフォーマンストレンド | クエリパフォーマンスは安定または改善しているか？ | P50/P95 レイテンシトレンド | 劣化している場合: 調査してチューニング |
| コスト追跡 | 実際のコストは予測予算内か？ | Cost Explorer データ | 予算超過の場合: 最適化またはリサイズ |
| プロセス改善 | 測定可能な時間/工数の節約があるか？ | Before/After プロセスメトリクス | 改善なしの場合: ユースケースフィットを再評価 |
| ユーザーフィードバック反映 | Day 30 のフィードバック項目に対応したか？ | 変更ログ | 未対応の場合: 優先対応 |

**60 日判断**: 最適化して継続 / より多くのユーザーに拡大 / アプローチを転換

### Day 90: バリューレポートとスケール判断

| チェック | 質問 | エビデンス | 判断 |
|-------|----------|----------|----------|
| ビジネス価値 | $ または時間の節約を定量化できるか？ | KPI ダッシュボード、コスト比較 | ポジティブな場合: エグゼクティブスポンサーに提示 |
| スケール準備度 | より多くのデータ/ユーザー/ユースケースに拡大できるか？ | キャパシティ評価、ユーザー需要 | 準備完了の場合: 拡大計画 |
| 運用成熟度 | Runbook はテスト済みか、モニタリングは安定しているか？ | 運用メトリクス、インシデント履歴 | 成熟している場合: BAU 運用に引き継ぎ |
| エグゼクティブレポート | リーダーシップ向けの説得力あるストーリーがあるか？ | 月次バリューレポート | 提示して拡大承認を取得 |

**90 日判断**: Scale / Maintain / Sunset

---

## 組織採用モデル

成功するデプロイメントには、技術だけでなく組織的なアラインメントが必要です。

### 役割と責任

| 役割 | 責任 | エンゲージメントレベル |
|------|---------------|-----------------|
| **エグゼクティブスポンサー** | 予算承認、組織優先度、ブロッカー除去 | 月次レビュー |
| **ビジネスオーナー** | ユースケース定義、成功基準、ユーザー受入 | 週次チェックイン |
| **データオーナー** | データアクセス承認、分類、保持 | プロジェクトごとの承認 |
| **プラットフォームチーム** | FSx + S3 AP + 分析のデプロイ、運用、モニタリング | 日次運用 |
| **セキュリティ / ガバナンス** | ポリシーレビュー、アクセス承認、監査 | 変更ごとのレビュー |
| **AI プロダクトオーナー** | RAG ユースケース定義、精度評価、ガードレール管理 | 週次イテレーション |
| **ユーザーチャンピオン** | ビジネスユニット内での採用推進、フィードバック収集 | 継続的 |
| **フィードバックコミュニティ** | 利用フィードバック提供、問題報告、改善提案 | 継続的 |

### 採用ステージ

```
Stage 1: Pilot (1 team, 1 use case)
  → Prove value with minimal risk
  → 1-2 months

Stage 2: Expand (multiple teams, same use case)
  → Scale proven pattern
  → 2-3 months

Stage 3: Diversify (multiple use cases)
  → Add RAG, new data sources, new analytics
  → 3-6 months

Stage 4: Standardize (organization-wide platform)
  → Self-service, governance automated, value tracked
  → 6-12 months
```

---

## Agentic AI 自動化禁止ルール

AI エージェントが機能を獲得するにつれ、人間の承認なしに決して自動化してはならないことを明示的に定義します。

| ルール | 根拠 | 強制方法 |
|------|-----------|-------------|
| 本番データを自律的に削除・変更**しない** | 不可逆；データ損失リスク | エージェント用読み取り専用 AP；エージェント IAM に DeleteObject/PutObject なし |
| 人間の承認なしにアクセスポリシーを変更**しない** | セキュリティ態勢の変更 | SCP が PutAccessPointPolicy を管理者ロールのみに制限 |
| 臨床的または財務的な意思決定を自律的に実行**しない** | 規制および安全リスク | 全意思決定出力に Human-in-the-loop 必須 |
| 承認された境界外に規制データをエクスポート**しない** | コンプライアンス違反 | VPC-origin AP；承認なしのクロスリージョン/クロスアカウント不可 |
| 承認なしに不可逆ワークフローをトリガー**しない** | 元に戻せない | エージェントがアクションを提案；人間が実行を承認 |
| 監査ログをバイパス**しない** | コンプライアンス要件 | エージェント IAM ロールで CloudTrail を無効化不可 |
| エージェントの分類レベルを超えるデータにアクセス**しない** | データガバナンス | エージェントごとの IAM を特定の AP とプレフィックスにスコープ |
| 定義されたコスト/スループット制限を超過**しない** | 予算管理 | サービスクォータとエージェントアクティビティの CloudWatch アラーム |

### 強制メカニズム

| メカニズム | 防止する内容 |
|-----------|-----------------|
| AP 上の読み取り専用ファイルシステムユーザー | エージェントがデータを書き込み/削除できない |
| スコープされた IAM ロール（GetObject + ListBucket のみ） | エージェントがポリシーやインフラを変更できない |
| エージェントアカウント/OU の SCP | エージェントが権限昇格できない |
| スループットの CloudWatch アラーム | 暴走エージェントを検出してスロットリング |
| Bedrock ガードレール | 不適切なコンテンツをフィルタリング |
| 人間承認ゲート | 不可逆アクションに明示的な承認が必要 |

---

## 参考資料

- [Amazon FSx for NetApp ONTAP のパフォーマンス](https://docs.aws.amazon.com/fsx/latest/ONTAPGuide/performance.html)
- [Amazon S3 アクセスポイント経由でのデータアクセス](https://docs.aws.amazon.com/fsx/latest/ONTAPGuide/accessing-data-via-s3-access-points.html)
- [AWS CloudTrail による FSx for ONTAP API コールのモニタリング](https://docs.aws.amazon.com/fsx/latest/ONTAPGuide/logging-using-cloudtrail-win.html)
- [Amazon Bedrock Knowledge Bases を使用した RAG アプリケーションの構築](https://docs.aws.amazon.com/fsx/latest/ONTAPGuide/tutorial-build-rag-with-bedrock.html)
- [AWS サービスでのアクセスポイントの使用](https://docs.aws.amazon.com/fsx/latest/ONTAPGuide/using-access-points-with-aws-services.html)
